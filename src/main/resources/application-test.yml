
server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: postgres

  jpa:
    properties:
      hibernate:
        ddl-auto: create
        default_schema: personsdb